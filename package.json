{
  "name": "south-bank",
  "version": "1.0.0",
  "private": true,
  "description": "South Bank web application",
  "scripts": {
    "start": "node server.js",
    "build": "node build-static.js",
    "check": "node --check server.js && node --check api/index.js && node --check build-static.js"
  },
  "engines": {
    "node": ">=22"
  }
}
